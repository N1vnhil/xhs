<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.n1vnhil.xhs.data.align.domain.mapper.UpdateMapper">

    <update id="updateUserFollowingTotalByUser">
        update t_user_count set following_total = #{followingTotal} where user_id=#{userId}
    </update>
    <update id="updateNoteLikeTotalByNoteId">
        update t_note_count set like_total = #{noteLikeTotal} where note_id = #{noteId}
    </update>
</mapper>